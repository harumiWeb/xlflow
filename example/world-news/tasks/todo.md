# Todo

1. Add the workbook-open event and the NewsAPI fetch pipeline.
2. Add a JSON parser plus a sheet renderer that can place article images from `urlToImage`.
3. Push, lint, test, run, save, and inspect the workbook result with xlflow session commands.
