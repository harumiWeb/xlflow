pre-commit:
  parallel: true
  commands:
    goimports:
      run: goimports -w cmd internal
    lint:
      run: golangci-lint run
    fmt:
      run: go fmt ./...
    pnpm-format:
      run: pnpm run format
