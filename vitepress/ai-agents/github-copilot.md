# GitHub Copilot

For GitHub Copilot-style repository instructions, install the shared agents target:

```bash
xlflow skill install --agent agents
```

This writes the bundled xlflow guidance under `.agents/skills/xlflow` so repository-aware agents can follow the same CLI workflow.
