# Claude Code

Install the Claude target when using Claude Code-style skill discovery:

```bash
xlflow skill install --agent claude
```

The same workflow rules apply: read `xlflow.toml`, edit source, push into the workbook, run lint/analyze/test/run, and save sessions explicitly.
