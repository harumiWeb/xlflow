# Build a News App

The news demo pattern fetches data, writes a table into Excel, and uses workbook inspection to verify rows and formatting.

Prefer a parameterized macro such as `News.Refresh` over prompts or file pickers so agents can run it headlessly.
