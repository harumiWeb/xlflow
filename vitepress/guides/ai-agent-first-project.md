# AI Agent First Project

1. Create or initialize a project.
2. Install the xlflow skill for the agent.
3. Run `xlflow doctor --json`.
4. Pull source before editing if freshness is unclear.
5. Edit files under `src/`.
6. Run `lint`, `analyze`, `push`, `test`, and `run`.
7. Save and stop the session when done.
