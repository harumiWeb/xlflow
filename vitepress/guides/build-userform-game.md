# Build a UserForm Game

UserForm-heavy projects should keep Designer structure in `src/forms/specs/*.yaml` and code-behind in the configured source mode. Use `form snapshot`, `form build --overwrite`, `inspect form`, and `form export-image` to verify the form.
