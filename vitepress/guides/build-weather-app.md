# Build a Weather App

Use xlflow to create the workbook, add VBA modules under `src/modules`, pass API inputs through arguments or configuration cells, then verify output with `inspect range` or `export-image`.

Keep network errors explicit in VBA so `run` can return actionable diagnostics.
