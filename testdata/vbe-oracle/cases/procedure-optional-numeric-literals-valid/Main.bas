Attribute VB_Name = "Main"
Option Explicit

Public Sub Probe( _
    Optional decimalValue As Long = 1&, _
    Optional integerValue As Integer = 1%, _
    Optional singleValue As Single = 1!, _
    Optional doubleValue As Double = 1#, _
    Optional currencyValue As Currency = 1@, _
    Optional longLongValue As LongLong = 1^, _
    Optional hexValue As Long = &H10, _
    Optional octalValue As Long = &O10)
End Sub
