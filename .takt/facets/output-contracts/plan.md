# タスク計画

以下の Markdown 本文のみを出力すること。

- 前置き、思考メモ、自己言及、説明文を出さない
- 1 行目は必ず `# タスク計画` で始める
- `The user wants me`、`Let me`、`I need to` のようなメタ会話を書かない
- 実装担当者が設計判断をしなくて済む粒度で書く
- ファイル名、関数名、型名、コマンド名、設定キーは、実際にリポジトリを確認したものだけを書く
- 未確認の名称を推測で書かない
- 不確実な場合は「該当する既存パターンを確認して合わせる」と明記する

## 元の要求

{ユーザーの要求をそのまま記載}

## 分析結果

### 目的

{達成すべきこと}

### 分解した要件

| #   | 要件    | 種別        | 備考   |
| --- | ------- | ----------- | ------ |
| 1   | {要件1} | 明示 / 暗黙 | {補足} |

### 参照資料の調査結果（参照資料がある場合）

{参照資料の実装アプローチの概要と、現在の実装との主要な差異。なければ「なし」}

### リポジトリ調査結果

| 確認対象         | 確認した内容                                                | 実装への影響         |
| ---------------- | ----------------------------------------------------------- | -------------------- |
| tasks/lessons.md | {該当する lesson。なければ「該当なし」}                     | {反映内容}           |
| 既存 spec / ADR  | {確認した docs/specs または docs/adr。なければ「該当なし」} | {反映内容}           |
| 近傍コード       | {確認したファイル・関数・型}                                | {再利用するパターン} |
| 既存テスト       | {確認したテスト}                                            | {追加・更新方針}     |

### スコープ

| 区分       | 対象                                        | 内容       |
| ---------- | ------------------------------------------- | ---------- |
| Go         | {対象ファイル/パッケージ。なければ「なし」} | {変更内容} |
| PowerShell | {対象ファイル。なければ「なし」}            | {変更内容} |
| DotNet     | {対象ファイル。なければ「なし」}            | {変更内容} |
| VBA        | {対象ファイル/モジュール。なければ「なし」} | {変更内容} |
| CLI        | {対象コマンド。なければ「なし」}            | {変更内容} |
| JSON 出力  | {対象。なければ「なし」}                    | {変更内容} |
| Docs       | {対象ファイル。なければ「なし」}            | {変更内容} |
| Tests      | {対象テスト。なければ「なし」}              | {変更内容} |

### 検討したアプローチ（設計判断がある場合）

| アプローチ | 採否          | 理由   |
| ---------- | ------------- | ------ |
| {案1}      | 採用 / 不採用 | {理由} |

### 採用する実装方針

{採用する設計方針を明確に記載}

### 到達経路・起動条件（利用者向け機能の追加/変更がある場合）

| 項目                         | 内容                         |
| ---------------------------- | ---------------------------- |
| 利用者が到達する入口         | {入口。なければ「入口なし」} |
| 更新が必要な呼び出し元・配線 | {更新対象}                   |
| 起動条件                     | {条件}                       |
| 未対応項目                   | {残件。なければ「なし」}     |

## 実装ブリーフ

このセクションは implementation step への主要な引き継ぎである。
実装担当者がアーキテクチャ判断をしなくて済む粒度で書くこと。

### 事前に読むファイル

| ファイル | 読む目的 |
| -------- | -------- |
| {path}   | {目的}   |

### 変更するファイル

| ファイル | 変更内容   | 注意点   |
| -------- | ---------- | -------- |
| {path}   | {変更内容} | {注意点} |

### 再利用する既存実装・パターン

| 対象                          | 再利用する理由 | 適用方法         |
| ----------------------------- | -------------- | ---------------- |
| {関数/型/パッケージ/既存実装} | {理由}         | {どう合わせるか} |

### 実装手順

1. {最初に行う変更}
2. {次に行う変更}
3. {テストまたは配線の更新}
4. {ドキュメント更新。不要なら「不要」}

### CLI / API / JSON 契約

| 項目       | 契約                                                       |
| ---------- | ---------------------------------------------------------- |
| 入力       | {引数、オプション、設定、ファイルなど}                     |
| 出力       | {標準出力、JSON、生成物など}                               |
| エラー     | {エラー条件、メッセージ、exit code、JSON diagnostics など} |
| 後方互換性 | {維持すべき互換性}                                         |

### Excel / COM / VBIDE / Workbook 互換性

{該当する場合は、Excel COM、VBIDE、UserForm、session、push、run、test、pull、save、workbook 互換性への影響を書く。該当しない場合は「該当なし」}

### 非ゴール

- {今回やらないこと}

### 禁止する変更

- {広範なリファクタ禁止、公開契約変更禁止、無関係な整形禁止など}

### 判断に迷った場合の戻し先

以下に該当する場合、implementation step は独自判断で設計を変更せず、plan step に戻すこと。

- 想定したファイル、関数、型、設定が存在しない
- 既存実装が plan の前提と矛盾している
- CLI / JSON / workbook 互換性に影響する追加判断が必要
- テスト失敗の修正にスコープ外の変更が必要
- Excel COM / VBIDE / UserForm / session 周りで E2E 前提が崩れる

## テスト計画

### 追加・更新するテスト

| テスト              | 目的               | 期待結果   |
| ------------------- | ------------------ | ---------- |
| {テスト名/ファイル} | {検証する振る舞い} | {期待結果} |

### 実行する検証コマンド

```bash
{command}
```

### E2E 検証

{Excel COM、VBIDE、UserForm、session、push、run、test、pull、save に関わる場合は必須。該当しない場合は「該当なし」}

## ドキュメント更新

| 対象         | 更新要否    | 理由   |
| ------------ | ----------- | ------ |
| docs/specs/  | 必要 / 不要 | {理由} |
| docs/adr/    | 必要 / 不要 | {理由} |
| vitepress/   | 必要 / 不要 | {理由} |
| README.md    | 必要 / 不要 | {理由} |
| CHANGELOG.md | 必要 / 不要 | {理由} |

## リスク分析

| リスク   | 影響   | 対策   |
| -------- | ------ | ------ |
| {リスク} | {影響} | {対策} |

## 確認事項（あれば）

- {確認事項。なければ「なし」}
